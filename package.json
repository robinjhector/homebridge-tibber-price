{
  "private": true,
  "displayName": "Homebridge Tibber Price",
  "name": "homebridge-tibber-price",
  "version": "1.0.0",
  "description": "Functionality based on Tibber Price (Hourly spot prices). Has the ability to register sensors that triggers for ",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git@github.com:robinjhector/homebridge-tibber-price.git"
  },
  "bugs": {
    "url": "https://github.com/robinjhector/homebridge-tibber-price/issues"
  },
  "engines": {
    "node": ">=14.18.1",
    "homebridge": ">=1.5.0"
  },
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint src/**.ts --max-warnings=0",
    "watch": "npm run build && npm link && nodemon",
    "build": "rimraf ./dist && tsc",
    "prepublishOnly": "npm run lint && npm run build"
  },
  "keywords": [
    "homebridge-plugin"
  ],
  "dependencies": {
    "tibber-api": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^16.10.9",
    "@typescript-eslint/eslint-plugin": "^5.0.0",
    "@typescript-eslint/parser": "^5.0.0",
    "eslint": "^8.0.1",
    "homebridge": "^1.5.0",
    "homebridge-config-ui-x": "^4.50.1",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.4.4"
  }
}
